#set-option -ug buffer-limit
#set-option -u buffer-limit
#set-option -ug default-terminal
#set-option -u default-terminal
#set-option -ug escape-time
#set-option -u escape-time
#set-option -ug exit-empty
#set-option -u exit-empty
#set-option -ug exit-unattached
#set-option -u exit-unattached
#set-option -ug focus-events
#set-option -u focus-events
#set-option -ug history-file
#set-option -u history-file
#set-option -ug message-limit
#set-option -u message-limit
#set-option -ug set-clipboard
#set-option -u set-clipboard
#set-option -ug activity-action
#set-option -u activity-action
#set-option -ug assume-paste-time
#set-option -u assume-paste-time
#set-option -ug base-index
#set-option -u base-index
#set-option -ug bell-action
#set-option -u bell-action
#set-option -ug default-command
#set-option -u default-command
#set-option -ug default-size
#set-option -u default-size
#set-option -ug destroy-unattached
#set-option -u destroy-unattached
#set-option -ug detach-on-destroy
#set-option -u detach-on-destroy
#set-option -ug display-panes-active-colour
#set-option -u display-panes-active-colour
#set-option -ug display-panes-colour
#set-option -u display-panes-colour
#set-option -ug display-panes-time
#set-option -u display-panes-time
#set-option -ug display-time
#set-option -u display-time
#set-option -ug history-limit
#set-option -u history-limit
#set-option -ug key-table
#set-option -u key-table
#set-option -ug lock-after-time
#set-option -u lock-after-time
#set-option -ug lock-command
#set-option -u lock-command
#set-option -ug message-command-style
#set-option -u message-command-style
#set-option -ug message-style
#set-option -u message-style
#set-option -ug mouse
#set-option -u mouse
#set-option -ug prefix
#set-option -u prefix
#set-option -ug prefix2
#set-option -u prefix2
#set-option -ug renumber-windows
#set-option -u renumber-windows
#set-option -ug repeat-time
#set-option -u repeat-time
#set-option -ug set-titles
#set-option -u set-titles
#set-option -ug set-titles-string
#set-option -u set-titles-string
#set-option -ug silence-action
#set-option -u silence-action
#set-option -ug status
#set-option -u status
#set-option -ug status-interval
#set-option -u status-interval
#set-option -ug status-justify
#set-option -u status-justify
#set-option -ug status-keys
#set-option -u status-keys
#set-option -ug status-left
#set-option -u status-left
#set-option -ug status-left-length
#set-option -u status-left-length
#set-option -ug status-left-style
#set-option -u status-left-style
#set-option -ug status-position
#set-option -u status-position
#set-option -ug status-right
#set-option -u status-right
#set-option -ug status-right-length
#set-option -u status-right-length
#set-option -ug status-right-style
#set-option -u status-right-style
#set-option -ug status-style
#set-option -u status-style
#set-option -ug visual-activity
#set-option -u visual-activity
#set-option -ug visual-bell
#set-option -u visual-bell
#set-option -ug visual-silence
#set-option -u visual-silence
#set-option -ug word-separators
#set-option -u word-separators
#set-option -ug aggressive-resize
#set-option -u aggressive-resize
#set-option -ug automatic-rename
#set-option -u automatic-rename
#set-option -ug automatic-rename-format
#set-option -u automatic-rename-format
#set-option -ug clock-mode-colour
#set-option -u clock-mode-colour
#set-option -ug clock-mode-style
#set-option -u clock-mode-style
#set-option -ug main-pane-height
#set-option -u main-pane-height
#set-option -ug main-pane-width
#set-option -u main-pane-width
#set-option -ug mode-keys
#set-option -u mode-keys
#set-option -ug mode-style
#set-option -u mode-style
#set-option -ug monitor-activity
#set-option -u monitor-activity
#set-option -ug monitor-bell
#set-option -u monitor-bell
#set-option -ug monitor-silence
#set-option -u monitor-silence
#set-option -ug other-pane-height
#set-option -u other-pane-height
#set-option -ug other-pane-width
#set-option -u other-pane-width
#set-option -ug pane-active-border-style
#set-option -u pane-active-border-style
#set-option -ug pane-base-index
#set-option -u pane-base-index
#set-option -ug pane-border-format
#set-option -u pane-border-format
#set-option -ug pane-border-status
#set-option -u pane-border-status
#set-option -ug pane-border-style
#set-option -u pane-border-style
#set-option -ug synchronize-panes
#set-option -u synchronize-panes
#set-option -ug window-status-activity-style
#set-option -u window-status-activity-style
#set-option -ug window-status-bell-style
#set-option -u window-status-bell-style
#set-option -ug window-status-current-format
#set-option -u window-status-current-format
#set-option -ug window-status-current-style
#set-option -u window-status-current-style
#set-option -ug window-status-format
#set-option -u window-status-format
#set-option -ug window-status-last-style
#set-option -u window-status-last-style
#set-option -ug window-status-separator
#set-option -u window-status-separator
#set-option -ug window-status-style
#set-option -u window-status-style
#set-option -ug window-size
#set-option -u window-size
#set-option -ug wrap-search
#set-option -u wrap-search
#set-option -ug xterm-keys
#set-option -u xterm-keys
#set-option -ug allow-rename
#set-option -u allow-rename
#set-option -ug alternate-screen
#set-option -u alternate-screen
#set-option -ug remain-on-exit
#set-option -u remain-on-exit
#set-option -ug window-active-style
#set-option -u window-active-style
#set-option -ug window-style
#set-option -u window-style
#unbind-key -a
#bind-key C-b send-prefix
#bind-key C-o rotate-window
#bind-key C-z suspend-client
#bind-key Space next-layout
#bind-key ! break-pane
#bind-key '"' split-window
#bind-key '#' list-buffers
#bind-key '$' command-prompt -I'#S' "rename-session -- '%%'"
#bind-key % split-window -h
#bind-key & confirm-before -p"kill-window #W? (y/n)" kill-window
#bind-key "'" command-prompt -pindex "select-window -t ':%%'"
#bind-key ( switch-client -p
#bind-key ) switch-client -n
#bind-key , command-prompt -I'#W' "rename-window -- '%%'"
#bind-key - delete-buffer
#bind-key . command-prompt "move-window -t '%%'"
#bind-key 0 select-window -t:=0
#bind-key 1 select-window -t:=1
#bind-key 2 select-window -t:=2
#bind-key 3 select-window -t:=3
#bind-key 4 select-window -t:=4
#bind-key 5 select-window -t:=5
#bind-key 6 select-window -t:=6
#bind-key 7 select-window -t:=7
#bind-key 8 select-window -t:=8
#bind-key 9 select-window -t:=9
#bind-key : command-prompt
#bind-key '\;' last-pane
#bind-key = choose-buffer -Z
#bind-key ? list-keys
#bind-key D choose-client -Z
#bind-key E select-layout -E
#bind-key L switch-client -l
#bind-key M select-pane -M
#bind-key [ copy-mode
#bind-key ] paste-buffer
#bind-key c new-window
#bind-key d detach-client
#bind-key f command-prompt "find-window -Z -- '%%'"
#bind-key i display-message
#bind-key l last-window
#bind-key m select-pane -m
#bind-key n next-window
#bind-key o select-pane -t:.+
#bind-key p previous-window
#bind-key q display-panes
#bind-key r refresh-client
#bind-key s choose-tree -Zs
#bind-key t clock-mode
#bind-key w choose-tree -Zw
#bind-key x confirm-before -p"kill-pane #P? (y/n)" kill-pane
#bind-key z resize-pane -Z
#bind-key { swap-pane -U
#bind-key } swap-pane -D
#bind-key '~' show-messages
#bind-key PPage copy-mode -u
#bind-key -r Up select-pane -U
#bind-key -r Down select-pane -D
#bind-key -r Left select-pane -L
#bind-key -r Right select-pane -R
#bind-key M-1 select-layout even-horizontal
#bind-key M-2 select-layout even-vertical
#bind-key M-3 select-layout main-horizontal
#bind-key M-4 select-layout main-vertical
#bind-key M-5 select-layout tiled
#bind-key M-n next-window -a
#bind-key M-o rotate-window -D
#bind-key M-p previous-window -a
#bind-key -r S-Up refresh-client -U 10
#bind-key -r S-Down refresh-client -D 10
#bind-key -r S-Left refresh-client -L 10
#bind-key -r S-Right refresh-client -R 10
#bind-key -r DC refresh-client -c
#bind-key -r M-Up resize-pane -U 5
#bind-key -r M-Down resize-pane -D 5
#bind-key -r M-Left resize-pane -L 5
#bind-key -r M-Right resize-pane -R 5
#bind-key -r C-Up resize-pane -U
#bind-key -r C-Down resize-pane -D
#bind-key -r C-Left resize-pane -L
#bind-key -r C-Right resize-pane -R
#bind-key -n MouseDown1Pane select-pane -t=\; send-keys -M
#bind-key -n MouseDrag1Border resize-pane -M
#bind-key -n MouseDown1Status select-window -t=
#bind-key -n WheelDownStatus next-window
#bind-key -n WheelUpStatus previous-window
#bind-key -n MouseDrag1Pane if -Ft= '#{mouse_any_flag}' 'if -Ft= "#{pane_in_mode}" "copy-mode -M" "send-keys -M"' 'copy-mode -M'
#bind-key -n MouseDown3Pane if-shell -Ft= '#{mouse_any_flag}' 'select-pane -t=; send-keys -M' 'select-pane -mt='
#bind-key -n WheelUpPane if-shell -Ft= '#{mouse_any_flag}' 'send-keys -M' 'if -Ft= "#{pane_in_mode}" "send-keys -M" "copy-mode -et="'
#bind-key -Tcopy-mode C-Space send -X begin-selection
#bind-key -Tcopy-mode C-a send -X start-of-line
#bind-key -Tcopy-mode C-c send -X cancel
#bind-key -Tcopy-mode C-e send -X end-of-line
#bind-key -Tcopy-mode C-f send -X cursor-right
#bind-key -Tcopy-mode C-b send -X cursor-left
#bind-key -Tcopy-mode C-g send -X clear-selection
#bind-key -Tcopy-mode C-k send -X copy-end-of-line
#bind-key -Tcopy-mode C-n send -X cursor-down
#bind-key -Tcopy-mode C-p send -X cursor-up
#bind-key -Tcopy-mode C-r command-prompt -ip'(search up)' -I'#{pane_search_string}' 'send -X search-backward-incremental "%%%"'
#bind-key -Tcopy-mode C-s command-prompt -ip'(search down)' -I'#{pane_search_string}' 'send -X search-forward-incremental "%%%"'
#bind-key -Tcopy-mode C-v send -X page-down
#bind-key -Tcopy-mode C-w send -X copy-selection-and-cancel
#bind-key -Tcopy-mode Escape send -X cancel
#bind-key -Tcopy-mode Space send -X page-down
#bind-key -Tcopy-mode , send -X jump-reverse
#bind-key -Tcopy-mode '\;' send -X jump-again
#bind-key -Tcopy-mode F command-prompt -1p'(jump backward)' 'send -X jump-backward "%%%"'
#bind-key -Tcopy-mode N send -X search-reverse
#bind-key -Tcopy-mode R send -X rectangle-toggle
#bind-key -Tcopy-mode T command-prompt -1p'(jump to backward)' 'send -X jump-to-backward "%%%"'
#bind-key -Tcopy-mode f command-prompt -1p'(jump forward)' 'send -X jump-forward "%%%"'
#bind-key -Tcopy-mode g command-prompt -p'(goto line)' 'send -X goto-line "%%%"'
#bind-key -Tcopy-mode n send -X search-again
#bind-key -Tcopy-mode q send -X cancel
#bind-key -Tcopy-mode t command-prompt -1p'(jump to forward)' 'send -X jump-to-forward "%%%"'
#bind-key -Tcopy-mode Home send -X start-of-line
#bind-key -Tcopy-mode End send -X end-of-line
#bind-key -Tcopy-mode MouseDown1Pane select-pane
#bind-key -Tcopy-mode MouseDrag1Pane select-pane\; send -X begin-selection
#bind-key -Tcopy-mode MouseDragEnd1Pane send -X copy-selection-and-cancel
#bind-key -Tcopy-mode WheelUpPane select-pane\; send -N5 -X scroll-up
#bind-key -Tcopy-mode WheelDownPane select-pane\; send -N5 -X scroll-down
#bind-key -Tcopy-mode DoubleClick1Pane select-pane\; send -X select-word
#bind-key -Tcopy-mode TripleClick1Pane select-pane\; send -X select-line
#bind-key -Tcopy-mode NPage send -X page-down
#bind-key -Tcopy-mode PPage send -X page-up
#bind-key -Tcopy-mode Up send -X cursor-up
#bind-key -Tcopy-mode Down send -X cursor-down
#bind-key -Tcopy-mode Left send -X cursor-left
#bind-key -Tcopy-mode Right send -X cursor-right
#bind-key -Tcopy-mode M-1 command-prompt -Np'(repeat)' -I1 'send -N "%%%"'
#bind-key -Tcopy-mode M-2 command-prompt -Np'(repeat)' -I2 'send -N "%%%"'
#bind-key -Tcopy-mode M-3 command-prompt -Np'(repeat)' -I3 'send -N "%%%"'
#bind-key -Tcopy-mode M-4 command-prompt -Np'(repeat)' -I4 'send -N "%%%"'
#bind-key -Tcopy-mode M-5 command-prompt -Np'(repeat)' -I5 'send -N "%%%"'
#bind-key -Tcopy-mode M-6 command-prompt -Np'(repeat)' -I6 'send -N "%%%"'
#bind-key -Tcopy-mode M-7 command-prompt -Np'(repeat)' -I7 'send -N "%%%"'
#bind-key -Tcopy-mode M-8 command-prompt -Np'(repeat)' -I8 'send -N "%%%"'
#bind-key -Tcopy-mode M-9 command-prompt -Np'(repeat)' -I9 'send -N "%%%"'
#bind-key -Tcopy-mode M-< send -X history-top
#bind-key -Tcopy-mode M-> send -X history-bottom
#bind-key -Tcopy-mode M-R send -X top-line
#bind-key -Tcopy-mode M-b send -X previous-word
#bind-key -Tcopy-mode M-f send -X next-word-end
#bind-key -Tcopy-mode M-m send -X back-to-indentation
#bind-key -Tcopy-mode M-r send -X middle-line
#bind-key -Tcopy-mode M-v send -X page-up
#bind-key -Tcopy-mode M-w send -X copy-selection-and-cancel
#bind-key -Tcopy-mode M-{ send -X previous-paragraph
#bind-key -Tcopy-mode M-} send -X next-paragraph
#bind-key -Tcopy-mode M-Up send -X halfpage-up
#bind-key -Tcopy-mode M-Down send -X halfpage-down
#bind-key -Tcopy-mode C-Up send -X scroll-up
#bind-key -Tcopy-mode C-Down send -X scroll-down
#bind-key -Tcopy-mode-vi C-c send -X cancel
#bind-key -Tcopy-mode-vi C-d send -X halfpage-down
#bind-key -Tcopy-mode-vi C-e send -X scroll-down
#bind-key -Tcopy-mode-vi C-b send -X page-up
#bind-key -Tcopy-mode-vi C-f send -X page-down
#bind-key -Tcopy-mode-vi C-h send -X cursor-left
#bind-key -Tcopy-mode-vi C-j send -X copy-selection-and-cancel
#bind-key -Tcopy-mode-vi Enter send -X copy-selection-and-cancel
#bind-key -Tcopy-mode-vi C-u send -X halfpage-up
#bind-key -Tcopy-mode-vi C-v send -X rectangle-toggle
#bind-key -Tcopy-mode-vi C-y send -X scroll-up
#bind-key -Tcopy-mode-vi Escape send -X clear-selection
#bind-key -Tcopy-mode-vi Space send -X begin-selection
#bind-key -Tcopy-mode-vi '$' send -X end-of-line
#bind-key -Tcopy-mode-vi , send -X jump-reverse
#bind-key -Tcopy-mode-vi / command-prompt -p'(search down)' 'send -X search-forward "%%%"'
#bind-key -Tcopy-mode-vi 0 send -X start-of-line
#bind-key -Tcopy-mode-vi 1 command-prompt -Np'(repeat)' -I1 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 2 command-prompt -Np'(repeat)' -I2 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 3 command-prompt -Np'(repeat)' -I3 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 4 command-prompt -Np'(repeat)' -I4 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 5 command-prompt -Np'(repeat)' -I5 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 6 command-prompt -Np'(repeat)' -I6 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 7 command-prompt -Np'(repeat)' -I7 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 8 command-prompt -Np'(repeat)' -I8 'send -N "%%%"'
#bind-key -Tcopy-mode-vi 9 command-prompt -Np'(repeat)' -I9 'send -N "%%%"'
#bind-key -Tcopy-mode-vi : command-prompt -p'(goto line)' 'send -X goto-line "%%%"'
#bind-key -Tcopy-mode-vi '\;' send -X jump-again
#bind-key -Tcopy-mode-vi ? command-prompt -p'(search up)' 'send -X search-backward "%%%"'
#bind-key -Tcopy-mode-vi A send -X append-selection-and-cancel
#bind-key -Tcopy-mode-vi B send -X previous-space
#bind-key -Tcopy-mode-vi D send -X copy-end-of-line
#bind-key -Tcopy-mode-vi E send -X next-space-end
#bind-key -Tcopy-mode-vi F command-prompt -1p'(jump backward)' 'send -X jump-backward "%%%"'
#bind-key -Tcopy-mode-vi G send -X history-bottom
#bind-key -Tcopy-mode-vi H send -X top-line
#bind-key -Tcopy-mode-vi J send -X scroll-down
#bind-key -Tcopy-mode-vi K send -X scroll-up
#bind-key -Tcopy-mode-vi L send -X bottom-line
#bind-key -Tcopy-mode-vi M send -X middle-line
#bind-key -Tcopy-mode-vi N send -X search-reverse
#bind-key -Tcopy-mode-vi T command-prompt -1p'(jump to backward)' 'send -X jump-to-backward "%%%"'
#bind-key -Tcopy-mode-vi V send -X select-line
#bind-key -Tcopy-mode-vi W send -X next-space
#bind-key -Tcopy-mode-vi ^ send -X back-to-indentation
#bind-key -Tcopy-mode-vi b send -X previous-word
#bind-key -Tcopy-mode-vi e send -X next-word-end
#bind-key -Tcopy-mode-vi f command-prompt -1p'(jump forward)' 'send -X jump-forward "%%%"'
#bind-key -Tcopy-mode-vi g send -X history-top
#bind-key -Tcopy-mode-vi h send -X cursor-left
#bind-key -Tcopy-mode-vi j send -X cursor-down
#bind-key -Tcopy-mode-vi k send -X cursor-up
#bind-key -Tcopy-mode-vi l send -X cursor-right
#bind-key -Tcopy-mode-vi n send -X search-again
#bind-key -Tcopy-mode-vi o send -X other-end
#bind-key -Tcopy-mode-vi q send -X cancel
#bind-key -Tcopy-mode-vi t command-prompt -1p'(jump to forward)' 'send -X jump-to-forward "%%%"'
#bind-key -Tcopy-mode-vi v send -X rectangle-toggle
#bind-key -Tcopy-mode-vi w send -X next-word
#bind-key -Tcopy-mode-vi { send -X previous-paragraph
#bind-key -Tcopy-mode-vi } send -X next-paragraph
#bind-key -Tcopy-mode-vi MouseDown1Pane select-pane
#bind-key -Tcopy-mode-vi MouseDrag1Pane select-pane\; send -X begin-selection
#bind-key -Tcopy-mode-vi MouseDragEnd1Pane send -X copy-selection-and-cancel
#bind-key -Tcopy-mode-vi WheelUpPane select-pane\; send -N5 -X scroll-up
#bind-key -Tcopy-mode-vi WheelDownPane select-pane\; send -N5 -X scroll-down
#bind-key -Tcopy-mode-vi DoubleClick1Pane select-pane\; send -X select-word
#bind-key -Tcopy-mode-vi TripleClick1Pane select-pane\; send -X select-line
#bind-key -Tcopy-mode-vi BSpace send -X cursor-left
#bind-key -Tcopy-mode-vi NPage send -X page-down
#bind-key -Tcopy-mode-vi PPage send -X page-up
#bind-key -Tcopy-mode-vi Up send -X cursor-up
#bind-key -Tcopy-mode-vi Down send -X cursor-down
#bind-key -Tcopy-mode-vi Left send -X cursor-left
#bind-key -Tcopy-mode-vi Right send -X cursor-right
#bind-key -Tcopy-mode-vi C-Up send -X scroll-up
#bind-key -Tcopy-mode-vi C-Down send -X scroll-down




bind-key    -T copy-mode    C-Space           send-keys -X begin-selection
bind-key    -T copy-mode    C-a               send-keys -X start-of-line
bind-key    -T copy-mode    C-b               send-keys -X cursor-left
bind-key    -T copy-mode    C-c               send-keys -X cancel
bind-key    -T copy-mode    C-e               send-keys -X end-of-line
bind-key    -T copy-mode    C-f               send-keys -X cursor-right
bind-key    -T copy-mode    C-g               send-keys -X clear-selection
bind-key    -T copy-mode    C-k               send-keys -X copy-end-of-line
bind-key    -T copy-mode    C-n               send-keys -X cursor-down
bind-key    -T copy-mode    C-p               send-keys -X cursor-up
bind-key    -T copy-mode    C-r               command-prompt -i -I "#{pane_search_string}" -p "(search up)" "send -X search-backward-incremental \"%%%\""
bind-key    -T copy-mode    C-s               command-prompt -i -I "#{pane_search_string}" -p "(search down)" "send -X search-forward-incremental \"%%%\""
bind-key    -T copy-mode    C-v               send-keys -X page-down
bind-key    -T copy-mode    C-w               send-keys -X copy-selection-and-cancel
bind-key    -T copy-mode    Escape            send-keys -X cancel
bind-key    -T copy-mode    Space             send-keys -X page-down
bind-key    -T copy-mode    ,                 send-keys -X jump-reverse
bind-key    -T copy-mode    \;                 send-keys -X jump-again
bind-key    -T copy-mode    F                 command-prompt -1 -p "(jump backward)" "send -X jump-backward \"%%%\""
bind-key    -T copy-mode    N                 send-keys -X search-reverse
bind-key    -T copy-mode    R                 send-keys -X rectangle-toggle
bind-key    -T copy-mode    T                 command-prompt -1 -p "(jump to backward)" "send -X jump-to-backward \"%%%\""
bind-key    -T copy-mode    f                 command-prompt -1 -p "(jump forward)" "send -X jump-forward \"%%%\""
bind-key    -T copy-mode    g                 command-prompt -p "(goto line)" "send -X goto-line \"%%%\""
bind-key    -T copy-mode    n                 send-keys -X search-again
bind-key    -T copy-mode    q                 send-keys -X cancel
bind-key    -T copy-mode    t                 command-prompt -1 -p "(jump to forward)" "send -X jump-to-forward \"%%%\""
bind-key    -T copy-mode    MouseDown1Pane    select-pane
bind-key    -T copy-mode    MouseDrag1Pane    select-pane \; send-keys -X begin-selection
bind-key    -T copy-mode    MouseDragEnd1Pane send-keys -X copy-selection-and-cancel
bind-key    -T copy-mode    WheelUpPane       select-pane \; send-keys -X -N 5 scroll-up
bind-key    -T copy-mode    WheelDownPane     select-pane \; send-keys -X -N 5 scroll-down
bind-key    -T copy-mode    DoubleClick1Pane  select-pane \; send-keys -X select-word
bind-key    -T copy-mode    TripleClick1Pane  select-pane \; send-keys -X select-line
bind-key    -T copy-mode    Home              send-keys -X start-of-line
bind-key    -T copy-mode    End               send-keys -X end-of-line
bind-key    -T copy-mode    NPage             send-keys -X page-down
bind-key    -T copy-mode    PPage             send-keys -X page-up
bind-key    -T copy-mode    Up                send-keys -X cursor-up
bind-key    -T copy-mode    Down              send-keys -X cursor-down
bind-key    -T copy-mode    Left              send-keys -X cursor-left
bind-key    -T copy-mode    Right             send-keys -X cursor-right
bind-key    -T copy-mode    M-1               command-prompt -N -I 1 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-2               command-prompt -N -I 2 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-3               command-prompt -N -I 3 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-4               command-prompt -N -I 4 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-5               command-prompt -N -I 5 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-6               command-prompt -N -I 6 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-7               command-prompt -N -I 7 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-8               command-prompt -N -I 8 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-9               command-prompt -N -I 9 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode    M-<               send-keys -X history-top
bind-key    -T copy-mode    M->               send-keys -X history-bottom
bind-key    -T copy-mode    M-R               send-keys -X top-line
bind-key    -T copy-mode    M-b               send-keys -X previous-word
bind-key    -T copy-mode    M-f               send-keys -X next-word-end
bind-key    -T copy-mode    M-m               send-keys -X back-to-indentation
bind-key    -T copy-mode    M-r               send-keys -X middle-line
bind-key    -T copy-mode    M-v               send-keys -X page-up
bind-key    -T copy-mode    M-w               send-keys -X copy-selection-and-cancel
bind-key    -T copy-mode    M-{               send-keys -X previous-paragraph
bind-key    -T copy-mode    M-}               send-keys -X next-paragraph
bind-key    -T copy-mode    M-Up              send-keys -X halfpage-up
bind-key    -T copy-mode    M-Down            send-keys -X halfpage-down
bind-key    -T copy-mode    C-Up              send-keys -X scroll-up
bind-key    -T copy-mode    C-Down            send-keys -X scroll-down
bind-key    -T copy-mode-vi C-b               send-keys -X page-up
bind-key    -T copy-mode-vi C-c               send-keys -X cancel
bind-key    -T copy-mode-vi C-d               send-keys -X halfpage-down
bind-key    -T copy-mode-vi C-e               send-keys -X scroll-down
bind-key    -T copy-mode-vi C-f               send-keys -X page-down
bind-key    -T copy-mode-vi C-h               send-keys -X cursor-left
bind-key    -T copy-mode-vi C-j               send-keys -X copy-selection-and-cancel
bind-key    -T copy-mode-vi Enter             send-keys -X copy-selection-and-cancel
bind-key    -T copy-mode-vi C-u               send-keys -X halfpage-up
bind-key    -T copy-mode-vi C-v               send-keys -X rectangle-toggle
bind-key    -T copy-mode-vi C-y               send-keys -X scroll-up
bind-key    -T copy-mode-vi Escape            send-keys -X clear-selection
bind-key    -T copy-mode-vi Space             send-keys -X begin-selection
bind-key    -T copy-mode-vi '$'                 send-keys -X end-of-line
bind-key    -T copy-mode-vi ,                 send-keys -X jump-reverse
bind-key    -T copy-mode-vi /                 command-prompt -p "(search down)" "send -X search-forward \"%%%\""
bind-key    -T copy-mode-vi 0                 send-keys -X start-of-line
bind-key    -T copy-mode-vi 1                 command-prompt -N -I 1 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 2                 command-prompt -N -I 2 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 3                 command-prompt -N -I 3 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 4                 command-prompt -N -I 4 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 5                 command-prompt -N -I 5 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 6                 command-prompt -N -I 6 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 7                 command-prompt -N -I 7 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 8                 command-prompt -N -I 8 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi 9                 command-prompt -N -I 9 -p (repeat) "send -N \"%%%\""
bind-key    -T copy-mode-vi :                 command-prompt -p "(goto line)" "send -X goto-line \"%%%\""
bind-key    -T copy-mode-vi \;                 send-keys -X jump-again
bind-key    -T copy-mode-vi ?                 command-prompt -p "(search up)" "send -X search-backward \"%%%\""
bind-key    -T copy-mode-vi A                 send-keys -X append-selection-and-cancel
bind-key    -T copy-mode-vi B                 send-keys -X previous-space
bind-key    -T copy-mode-vi D                 send-keys -X copy-end-of-line
bind-key    -T copy-mode-vi E                 send-keys -X next-space-end
bind-key    -T copy-mode-vi F                 command-prompt -1 -p "(jump backward)" "send -X jump-backward \"%%%\""
bind-key    -T copy-mode-vi G                 send-keys -X history-bottom
bind-key    -T copy-mode-vi H                 send-keys -X top-line
bind-key    -T copy-mode-vi J                 send-keys -X scroll-down
bind-key    -T copy-mode-vi K                 send-keys -X scroll-up
bind-key    -T copy-mode-vi L                 send-keys -X bottom-line
bind-key    -T copy-mode-vi M                 send-keys -X middle-line
bind-key    -T copy-mode-vi N                 send-keys -X search-reverse
bind-key    -T copy-mode-vi T                 command-prompt -1 -p "(jump to backward)" "send -X jump-to-backward \"%%%\""
bind-key    -T copy-mode-vi V                 send-keys -X select-line
bind-key    -T copy-mode-vi W                 send-keys -X next-space
bind-key    -T copy-mode-vi ^                 send-keys -X back-to-indentation
bind-key    -T copy-mode-vi b                 send-keys -X previous-word
bind-key    -T copy-mode-vi e                 send-keys -X next-word-end
bind-key    -T copy-mode-vi f                 command-prompt -1 -p "(jump forward)" "send -X jump-forward \"%%%\""
bind-key    -T copy-mode-vi g                 send-keys -X history-top
bind-key    -T copy-mode-vi h                 send-keys -X cursor-left
bind-key    -T copy-mode-vi j                 send-keys -X cursor-down
bind-key    -T copy-mode-vi k                 send-keys -X cursor-up
bind-key    -T copy-mode-vi l                 send-keys -X cursor-right
bind-key    -T copy-mode-vi n                 send-keys -X search-again
bind-key    -T copy-mode-vi o                 send-keys -X other-end
bind-key    -T copy-mode-vi q                 send-keys -X cancel
bind-key    -T copy-mode-vi t                 command-prompt -1 -p "(jump to forward)" "send -X jump-to-forward \"%%%\""
bind-key    -T copy-mode-vi v                 send-keys -X rectangle-toggle
bind-key    -T copy-mode-vi w                 send-keys -X next-word
bind-key    -T copy-mode-vi {                 send-keys -X previous-paragraph
bind-key    -T copy-mode-vi }                 send-keys -X next-paragraph
bind-key    -T copy-mode-vi MouseDown1Pane    select-pane
bind-key    -T copy-mode-vi MouseDrag1Pane    select-pane \; send-keys -X begin-selection
bind-key    -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-selection-and-cancel
bind-key    -T copy-mode-vi WheelUpPane       select-pane \; send-keys -X -N 5 scroll-up
bind-key    -T copy-mode-vi WheelDownPane     select-pane \; send-keys -X -N 5 scroll-down
bind-key    -T copy-mode-vi DoubleClick1Pane  select-pane \; send-keys -X select-word
bind-key    -T copy-mode-vi TripleClick1Pane  select-pane \; send-keys -X select-line
bind-key    -T copy-mode-vi BSpace            send-keys -X cursor-left
bind-key    -T copy-mode-vi NPage             send-keys -X page-down
bind-key    -T copy-mode-vi PPage             send-keys -X page-up
bind-key    -T copy-mode-vi Up                send-keys -X cursor-up
bind-key    -T copy-mode-vi Down              send-keys -X cursor-down
bind-key    -T copy-mode-vi Left              send-keys -X cursor-left
bind-key    -T copy-mode-vi Right             send-keys -X cursor-right
bind-key    -T copy-mode-vi C-Up              send-keys -X scroll-up
bind-key    -T copy-mode-vi C-Down            send-keys -X scroll-down
bind-key    -T prefix       C-b               send-prefix
bind-key    -T prefix       C-o               rotate-window
bind-key    -T prefix       C-z               suspend-client
bind-key    -T prefix       Space             next-layout
bind-key    -T prefix       !                 break-pane
bind-key    -T prefix       '"'                 split-window
bind-key    -T prefix       '#'                 list-buffers
bind-key    -T prefix       '$'                 command-prompt -I "#S" "rename-session '%%'"
bind-key    -T prefix       %                 split-window -h
bind-key    -T prefix       &                 confirm-before -p "kill-window #W? (y/n)" kill-window
bind-key    -T prefix       "'"                 command-prompt -p index "select-window -t ':%%'"
bind-key    -T prefix       (                 switch-client -p
bind-key    -T prefix       )                 switch-client -n
bind-key    -T prefix       ,                 command-prompt -I "#W" "rename-window '%%'"
bind-key    -T prefix       -                 delete-buffer
bind-key    -T prefix       .                 command-prompt "move-window -t '%%'"
bind-key    -T prefix       0                 select-window -t :=0
bind-key    -T prefix       1                 select-window -t :=1
bind-key    -T prefix       2                 select-window -t :=2
bind-key    -T prefix       3                 select-window -t :=3
bind-key    -T prefix       4                 select-window -t :=4
bind-key    -T prefix       5                 select-window -t :=5
bind-key    -T prefix       6                 select-window -t :=6
bind-key    -T prefix       7                 select-window -t :=7
bind-key    -T prefix       8                 select-window -t :=8
bind-key    -T prefix       9                 select-window -t :=9
bind-key    -T prefix       :                 command-prompt
bind-key    -T prefix       \;                 last-pane
bind-key    -T prefix       =                 choose-buffer
bind-key    -T prefix       ?                 list-keys
bind-key    -T prefix       D                 choose-client
bind-key    -T prefix       L                 switch-client -l
bind-key    -T prefix       M                 select-pane -M
bind-key    -T prefix       [                 copy-mode
bind-key    -T prefix       ]                 paste-buffer
bind-key    -T prefix       c                 new-window
bind-key    -T prefix       d                 detach-client
bind-key    -T prefix       f                 command-prompt "find-window '%%'"
bind-key    -T prefix       i                 display-message
bind-key    -T prefix       l                 last-window
bind-key    -T prefix       m                 select-pane -m
bind-key    -T prefix       n                 next-window
bind-key    -T prefix       o                 select-pane -t :.+
bind-key    -T prefix       p                 previous-window
bind-key    -T prefix       q                 display-panes
bind-key    -T prefix       r                 refresh-client
bind-key    -T prefix       s                 choose-tree -s
bind-key    -T prefix       t                 clock-mode
bind-key    -T prefix       w                 choose-tree -w
bind-key    -T prefix       x                 confirm-before -p "kill-pane #P? (y/n)" kill-pane
bind-key    -T prefix       z                 resize-pane -Z
bind-key    -T prefix       {                 swap-pane -U
bind-key    -T prefix       }                 swap-pane -D
bind-key    -T prefix       '~'                 show-messages
bind-key    -T prefix       PPage             copy-mode -u
bind-key -r -T prefix       Up                select-pane -U
bind-key -r -T prefix       Down              select-pane -D
bind-key -r -T prefix       Left              select-pane -L
bind-key -r -T prefix       Right             select-pane -R
bind-key    -T prefix       M-1               select-layout even-horizontal
bind-key    -T prefix       M-2               select-layout even-vertical
bind-key    -T prefix       M-3               select-layout main-horizontal
bind-key    -T prefix       M-4               select-layout main-vertical
bind-key    -T prefix       M-5               select-layout tiled
bind-key    -T prefix       M-n               next-window -a
bind-key    -T prefix       M-o               rotate-window -D
bind-key    -T prefix       M-p               previous-window -a
bind-key -r -T prefix       M-Up              resize-pane -U 5
bind-key -r -T prefix       M-Down            resize-pane -D 5
bind-key -r -T prefix       M-Left            resize-pane -L 5
bind-key -r -T prefix       M-Right           resize-pane -R 5
bind-key -r -T prefix       C-Up              resize-pane -U
bind-key -r -T prefix       C-Down            resize-pane -D
bind-key -r -T prefix       C-Left            resize-pane -L
bind-key -r -T prefix       C-Right           resize-pane -R
bind-key    -T root         MouseDown1Pane    select-pane -t = \; send-keys -M
bind-key    -T root         MouseDown1Status  select-window -t =
bind-key    -T root         MouseDown3Pane    if-shell -F -t = "#{mouse_any_flag}" "select-pane -t=; send-keys -M" "select-pane -mt="
bind-key    -T root         MouseDrag1Pane    if-shell -F -t = "#{mouse_any_flag}" "if -Ft= \"#{pane_in_mode}\" \"copy-mode -M\" \"send-keys -M\"" "copy-mode -M"
bind-key    -T root         MouseDrag1Border  resize-pane -M
bind-key    -T root         WheelUpPane       if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= \"#{pane_in_mode}\" \"send-keys -M\" \"copy-mode -et=\""
bind-key    -T root         WheelUpStatus     previous-window
bind-key    -T root         WheelDownStatus   next-window
